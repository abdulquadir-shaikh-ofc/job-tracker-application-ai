# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (optional - defaults to allow all origins)
# ALLOWED_ORIGINS=http://localhost:5173,https://yourusername.github.io

# Note: API keys are sent from the frontend, not stored here
# This keeps your key secure and in your control
